#:kivy 1.0.9
#:import vertex_instructions kivy.graphics.vertex_instructions




<Game>:
    name: "Game"
    Button:
        on_release: app.root.current = "StartScreen"
        text: "Home"
        font_size: 20
        size_hint: 0.1, 0.05
        pos_hint: {"x": 0, "top": 1}

    Button:
     #   on_release: root.anim_to_pos()
        text: "reset"
        font_size: 20
        size_hint: 0.1, 0.05
        pos_hint: {"x": 0.8, "top": 1}

    Asteroid:
        size_hint: None, None
        canvas:
            Rectangle:
                size: self.size
                pos: self.pos


<Asteroid>:


